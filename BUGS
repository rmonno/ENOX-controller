Topology Manager
* A network node with IP addr = 0.0.0.0 is created (probably it is an OF
  switch). Verify the host_auth and host_bind events handling

* Fixed host_leave events hadnling when multiple events are raised
